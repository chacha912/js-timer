/* eslint-disable prefer-const */
/* ==== DOMs ==== */

/* === state === */
let isRunning = false;
let lapsedTime = { hh: 0, mm: 0, ss: 0, ms: 0 };
let laps = [];

/* === functions === */

/* === event hadlers === */
